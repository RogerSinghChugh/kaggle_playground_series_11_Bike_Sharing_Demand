# kaggle_playground_series_11_Bike_Sharing_Demand

We first have to do these steps:
pip install -U pip setuptools wheel

and after pip install -r req
do pip install autogluon.tabular[all,tabicl,tabpfn,realmlp]